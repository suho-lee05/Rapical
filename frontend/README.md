
  # MVP UI Design

  This is a code bundle for MVP UI Design. The original project is available at https://www.figma.com/design/ACUdIOB9g0WMVROXPa55mb/MVP-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Create `.env` from `.env.example`.

  Run `npm run dev` to start the development server.
  